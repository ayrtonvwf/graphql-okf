{
  "name": "graphql-okf-bench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Benchmark harness measuring graphql-okf's effect on agent accuracy and token usage",
  "scripts": {
    "bench": "node --env-file-if-exists=.env --experimental-strip-types src/run.ts",
    "judge": "node --env-file-if-exists=.env --experimental-strip-types src/judge-cli.ts",
    "report": "node --experimental-strip-types src/report-cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.220",
    "@anthropic-ai/sdk": "^0.115.0",
    "@graphql-tools/mock": "^9.0.0",
    "@graphql-tools/schema": "^10.0.0",
    "graphql": "^16.11.0",
    "graphql-okf": "workspace:*",
    "graphql-yoga": "^5.10.0",
    "mcp-graphql": "2.0.4",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
