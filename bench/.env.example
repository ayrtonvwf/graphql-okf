# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# Required by `pnpm run bench` (agent under test) and `pnpm run judge` (grader).
ANTHROPIC_API_KEY=

# Keeps MCP tools available on turn one for every trial of the graphql-mcp scenario.
MCP_CONNECTION_NONBLOCKING=0
