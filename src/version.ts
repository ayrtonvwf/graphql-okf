/**
 * The OKF §7 actor identity written into `generated.by`. Major.minor only: the
 * full patch version would make every package release rewrite every concept in
 * every bundle. Kept in step with package.json by version.test.ts.
 */
export const PRODUCER = "graphql-okf/0.1";
